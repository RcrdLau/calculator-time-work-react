import React from "react";
import { HeaderContainer, Title } from "./styles";

const Header = () => {

    return (
        <HeaderContainer>
            <Title>Calculator TimeWork</Title>
        </HeaderContainer>
    );
}

export default Header;
